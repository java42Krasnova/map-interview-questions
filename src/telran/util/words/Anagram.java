package telran.util.words;

public class Anagram {
	/**
	 * 
	 * @param word
	 * @param anagram
	 * @return true if anagram contains a word comprising of only all letters from a given word (case insensitive)
	 * Implementation requirements: O[N], using only map and two passings's over(one is over word and other is the anagram)
	 */
public static boolean isAnagram(String word, String anagram) {
	//TODO
	return false;
}
}
